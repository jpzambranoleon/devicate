import { AppBar, Toolbar, Typography } from "@mui/material";

const settings = ["Logout"];

const Navbar = () => {
  return (
    <>
      <AppBar>
        <Toolbar>
          <Typography>Devicate</Typography>
        </Toolbar>
      </AppBar>
    </>
  );
};

export default Navbar;
